<div class='row mt-3'>
    <div class='col-12'>
        <h4> CHI TIẾT DANH MỤC</h4>
    </div>
    <div class='col-12'>
        {{#if category}}
            <table class='table table-striped table-hover'>
                <tbody>
                    <tr>
                        <th class='text-black' style='min-width: 180px;'>TÊN DANH MỤC</th>
                        <td class='min-width-400'>{{category.name}}</td>
                    </tr>
                    <tr>
                        <th class='text-black'>HIỂN THỊ TRANG CHỦ</th>
                        <td>{{#if category.isShow}}ĐANG HIỂN THỊ{{else}}KHÔNG HIỂN THỊ{{/if}}</td>
                    </tr>
                    <tr>
                        <th class='text-black'>ẢNH</th>
                        <td><img width='100%' src='{{category.thumb}}' alt='{{category.name}}' /></td>
                    </tr>
                    <tr>
                        <th class='text-black'>MÔ TẢ</th>
                        <td>{{{category.description}}}</td>
                    </tr>
                </tbody>
            </table>
        {{else}}
            <h4>KHÔNG CÓ DANH MỤC</h4>
        {{/if}}
    </div>
</div>